CREATE TABLE IF NOT EXISTS conta (
     id serial PRIMARY KEY,
     -- cla varchar(20),
     -- genero varchar(9),
     -- nivel int NOT NULL,
     -- valor money NOT NULL,
     add1 varchar(40),
     add2 varchar(40)
);
